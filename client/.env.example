# Only needed when the client and API are on different origins — a deployed build,
# essentially. Locally the default is `/api/v1` and Vite proxies it to :4000, so there
# is nothing to configure and no CORS to think about.
#
# VITE_API_BASE_URL=https://your-api.onrender.com/api/v1
